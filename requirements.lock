# Baseline lock snapshot used for reproducible v1.30 CI/local validation.
# Refresh deliberately during dependency maintenance; do not edit casually.
alembic==1.18.4
fastapi==0.141.1
httpx==0.28.1
pydantic==2.13.4
pytest==9.0.3
SQLAlchemy==2.0.50
starlette==1.3.1
uvicorn==0.48.0
# psycopg[binary] is intentionally installed from pyproject in CI because the
# binary subdependency is platform-specific.
